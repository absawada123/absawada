# .gitignore

# Dependencies
# This folder contains all of your project's dependencies.
# It can be recreated at any time by running `npm install`.
/node_modules

# Next.js build output
# This is the build cache and output from `npm run dev` or `npm run build`.
/.next

# Static export output
# This folder is generated by `next export` for static hosting (like GitHub Pages).
/out

# Production build folder (alternative to .next)
/build

# Test results
/coverage

# Environment variables
# These files contain secret API keys and should NEVER be committed.
.env
.env.local
.env.development.local
.env.production.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor and OS specific files
.vscode/
.idea/
.DS_Store
*.swp